---
description: "Compose/arrange songs in Ableton using the structured songwriter workflow"
---

Use the songwriter workflow defined in `skills/songwriter/SKILL.md`, `skills/songwriter/references/intake-mcq.md`, and `skills/songwriter/references/song-recipes.md`.

Execution requirements:
- Follow the intake protocol and production brief contract from the skill.
- Build directly in Ableton using available MCP tools.
- Use plugin-aware behavior (list/load plugins where available, fallback clearly when missing).
- Respect safety guardrails (no destructive actions without confirmation, no retry loops on blocked operations).
- Finish with the handoff format and revision options specified in the skill.

User request:
`$ARGUMENTS`
